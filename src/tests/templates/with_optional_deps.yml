refs:
    - pip://nmk-python!plugin.yml

config:
  pythonPackageOptionalRequirements:
      implem:
        - foo
        - bar
      tests:
        - pytest

  venvArchiveDeps:
    - ${PROJECTDIR}/foo_bar-1.2.3.whl
