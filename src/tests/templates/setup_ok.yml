refs:
    - pip://nmk-python!plugin.yml

config:
    pythonSetupCfgFiles:
        - ${BASEDIR}/setup1.cfg
        - ${BASEDIR}/setup2.cfg

    pythonSetupItems:
        ignoredIntValue: 12
        dummy:
            ymlContributedValue: foo
        anotherSection:
            foo: bar
            arrayOfValues:
                - abc
                - def
        addedSection:
            abc: def
